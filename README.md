
# Weekly Priority Matrix Pro (Python + Firebase)

A professional roadmap planner built with **FastAPI** and **Firebase Firestore**, featuring high-end **GSAP** animations.

## 🚀 Quick Start
1. `pip install -r requirements.txt`
2. Generate `serviceAccountKey.json` from Firebase Console.
3. `uvicorn main:app --reload`
